func fetch{{.ServiceCamel}}{{.ResourceCamel}}{{.AbsolutFieldPathCamel}}(_ context.Context, _ schema.ClientMeta, parent *schema.Resource, res chan interface{}) error {
    values := funk.Get(parent.Item, {{.RelativeFieldPathCamelWithDot | printf "%q"}})

	if funk.IsIteratee(values) {
		funk.ForEach(values, func(value interface{}) {
			res <- value
		})
	}

	return nil
}
